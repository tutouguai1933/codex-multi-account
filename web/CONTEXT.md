当前：`web` 已经接到真实后端接口，并完成了无头浏览器验证，确认页面能渲染、能通过 `/api` 代理读后端、能点击“导入当前 OpenClaw”和“切 OpenClaw”后看到页面状态变化。
停点：还没有加入自动化前端测试框架，浏览器验证目前是通过无头 Chromium 手工驱动完成的。
决定：继续保持单页控制台结构，不引入前端路由；开发期通过 Vite 代理访问后端；登录流程第一版通过页面按钮触发后端接口，再在服务终端完成交互。
