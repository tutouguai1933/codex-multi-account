当前：`Codex 文件` 面板里的“上下文窗口 / 自动压缩阈值”已经改成按 `k` 显示和输入，页面默认值改为 `400k / 300k`。当前运行中的 `~/.codex/config.toml` 也已同步成 `400000 / 300000`，所以设置页会直接显示 `400 / 300`。
停点：9001 已重启到最新代码；真实页面已验证这两个字段的标签已变成 `（k）`，并且输入框当前值就是 `400 / 300`。当前会话的 `resume` 归属仍在 `/home/djy/codex-multi-account`。
决定：这里只改设置页显示和默认值，不改后端接口字段结构；页面内部继续把值换算成真实数字再保存，避免影响账号切换主逻辑和现有 API。
