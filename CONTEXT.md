当前：这轮继续修首页“运行面板”的真实重叠问题。根因不是文字太长，而是卡片在实际宽度下仍被强制双列；现在已经改成按卡片自身宽度自动切单列，不会再在窄宽度下互相压住。
停点：项目仍运行在 `http://127.0.0.1:9001/`。前端构建和后端测试都已通过，真页面已经确认运行面板在当前宽度下会改成上下布局，矩形不再重叠；删除确认仍然正常。
决定：首页继续保持“高信息密度、强调状态和动作”的运维控制台方向，但布局切换要按卡片自身宽度处理，不能只按整页断点猜。
